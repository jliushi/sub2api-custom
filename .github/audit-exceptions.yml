version: 1
exceptions:
  - package: xlsx
    advisory: "GHSA-4r6h-8v6p-xvw6"
    severity: high
    reason: "Admin export only via dynamic import (CVE-2023-30533); npm public release has no patched version, SheetJS ships fixes only via cdn.sheetjs.com"
    mitigation: "Load only on export; restrict export permissions and data scope; plan migration to cdn.sheetjs.com build"
    expires_on: "2027-03-07"
    owner: "security@your-domain"
  - package: xlsx
    advisory: "GHSA-5pgg-2g8v-p4x9"
    severity: high
    reason: "Admin export only via dynamic import (CVE-2024-22363); npm public release has no patched version, SheetJS ships fixes only via cdn.sheetjs.com"
    mitigation: "Load only on export; restrict export permissions and data scope; plan migration to cdn.sheetjs.com build"
    expires_on: "2027-03-07"
    owner: "security@your-domain"
